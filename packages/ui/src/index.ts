// Public API surface for @petpooja/ui.
// Components are generated from Figma via the Claude Figma MCP server and
// re-exported here as they ship. Until the first component lands, this barrel
// is intentionally empty — `export {}` keeps the file a valid ES module.
export {};
